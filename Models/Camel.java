import java.text.SimpleDateFormat;

public class Camel extends PackAnimal{
    public Camel(String name, SimpleDateFormat date_of_birth, String command, int liftWeight) {
        super(name, date_of_birth, command, liftWeight);
    }
}