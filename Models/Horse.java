import java.text.SimpleDateFormat;

public class Horse extends PackAnimal{
    public Horse(String name, SimpleDateFormat date_of_birth, String command, int liftWeight) {
        super(name, date_of_birth, command, liftWeight);
    }
}