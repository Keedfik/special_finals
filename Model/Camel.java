package Model;
import java.text.SimpleDateFormat;

public class Camel extends PackAnimal{
    public Camel(String name, SimpleDateFormat date_of_birth, String command, String type) {
        super(name, date_of_birth, command, type);
    }
}